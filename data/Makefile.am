upmfconfdir = $(sysconfdir)
upmfconf_DATA = settings.xml

# Install example files, so Upmf can be tested
pkgdir = $(datadir)/upmf/packages
pkg_DATA = ktorrent.xml

EXTRA_DIST = $(upmfconf_DATA) $(pkg_DATA)
